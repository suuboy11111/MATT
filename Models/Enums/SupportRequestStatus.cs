namespace MaiAmTinhThuong.Models.Enums
{
    public enum SupportRequestStatus
    {
        Pending = 1,      // Chờ duyệt
        Approved = 2,     // Đã duyệt
        Rejected = 3,     // Từ chối
        InProgress = 4,   // Đang xử lý
        Completed = 5     // Hoàn thành
    }
}

