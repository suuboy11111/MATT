@model MaiAmTinhThuong.Helpers.PagedResult<MaiAmTinhThuong.Models.Supporter>
@using MaiAmTinhThuong.Models
@using System.Linq

@{
    ViewData["Title"] = "Quản lý Người Hỗ Trợ";
}

<div class="admin-container">
    <div class="container-fluid px-4">
        <!-- Page Header -->
        <div class="admin-page-header">
            <h2><i class="fas fa-hand-holding-heart me-3"></i>Quản Lý Người Hỗ Trợ</h2>
            <p>Duyệt và quản lý các người hỗ trợ trong hệ thống</p>
        </div>

        <!-- Search & Filter Form -->
        <div class="admin-search-form">
            <form method="get" asp-action="ManageSupporters" class="row g-3">
                <div class="col-md-4">
                    <div class="input-group">
                        <span class="input-group-text bg-white border-end-0">
                            <i class="fas fa-search text-muted"></i>
                        </span>
                        <input type="text" name="searchString" class="form-control admin-form-control border-start-0" 
                               placeholder="Tìm kiếm theo tên, SĐT, địa chỉ..." 
                               value="@ViewBag.SearchString" />
                    </div>
                </div>
                <div class="col-md-2">
                    <select name="filterApproved" class="form-select admin-form-control">
                        <option value="">Tất cả trạng thái</option>
                        <option value="approved" selected="@(ViewBag.FilterApproved == "approved")">Đã duyệt</option>
                        <option value="pending" selected="@(ViewBag.FilterApproved == "pending")">Chờ duyệt</option>
                    </select>
                </div>
                <div class="col-md-2">
                    <select name="pageSize" class="form-select admin-form-control">
                        @foreach (var option in ViewBag.PageSizeOptions as List<Microsoft.AspNetCore.Mvc.Rendering.SelectListItem>)
                        {
                            <option value="@option.Value" selected="@(option.Selected)">@option.Text / trang</option>
                        }
                    </select>
                </div>
                <div class="col-md-4">
                    <button type="submit" class="admin-btn admin-btn-primary">
                        <i class="fas fa-search"></i> Tìm kiếm
                    </button>
                    <a asp-action="ManageSupporters" class="admin-btn admin-btn-secondary">
                        <i class="fas fa-times"></i> Xóa bộ lọc
                    </a>
                </div>
            </form>
        </div>

        <!-- Table -->
        <div class="admin-table-wrapper">
            <table class="table admin-table">
                <thead>
                    <tr>
                        <th><i class="fas fa-user me-2"></i>Tên</th>
                        <th><i class="fas fa-birthday-cake me-2"></i>Tuổi</th>
                        <th><i class="fas fa-venus-mars me-2"></i>Giới Tính</th>
                        <th><i class="fas fa-phone me-2"></i>Số Điện Thoại</th>
                        <th><i class="fas fa-map-marker-alt me-2"></i>Địa Chỉ</th>
                        <th><i class="fas fa-heart me-2"></i>Loại Hỗ Trợ</th>
                        <th><i class="fas fa-info-circle me-2"></i>Trạng Thái</th>
                        <th class="text-center"><i class="fas fa-cog me-2"></i>Hành Động</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model.Items != null && Model.Items.Any())
                    {
                        @foreach (var supporter in Model.Items)
                        {
                            <tr>
                                <td><strong>@(supporter.Name ?? "N/A")</strong></td>
                                <td>@supporter.Age</td>
                                <td>@(supporter.Gender?.ToString() ?? "N/A")</td>
                                <td>@(supporter.PhoneNumber ?? "N/A")</td>
                                <td>@(supporter.Address ?? "N/A")</td>
                                <td>
                                    @{
                                        var supportTypes = supporter.SupporterSupportTypes?
                                            .Where(sst => sst?.SupportType != null)
                                            .Select(sst => sst.SupportType.Name)
                                            .ToList() ?? new List<string>();
                                    }
                                    @if (supportTypes.Any())
                                    {
                                        <span class="admin-badge admin-badge-info">
                                            <i class="fas fa-tags"></i> @string.Join(", ", supportTypes)
                                        </span>
                                    }
                                    else
                                    {
                                        <span class="text-muted">N/A</span>
                                    }
                                </td>
                                <td>
                                    @if (supporter.IsApproved)
                                    {
                                        <span class="admin-badge admin-badge-success">
                                            <i class="fas fa-check-circle"></i> Đã duyệt
                                        </span>
                                    }
                                    else
                                    {
                                        <span class="admin-badge admin-badge-warning">
                                            <i class="fas fa-clock"></i> Chờ duyệt
                                        </span>
                                    }
                                </td>
                                <td>
                                    <div class="d-flex gap-2 justify-content-center">
                                        <a asp-action="DetailsSupporter" asp-controller="MaiAmAdmin" asp-route-id="@supporter.Id" 
                                           class="admin-btn admin-btn-info admin-btn-sm">
                                            <i class="fas fa-eye"></i> Xem
                                        </a>
                                        <a asp-action="EditSupporter" asp-controller="MaiAmAdmin" asp-route-id="@supporter.Id" 
                                           class="admin-btn admin-btn-warning admin-btn-sm">
                                            <i class="fas fa-edit"></i> Sửa
                                        </a>
                                        <form asp-action="DeleteSupporter" method="post" style="display:inline;" 
                                              onsubmit="return confirm('Bạn có chắc muốn xóa?');">
                                            <input type="hidden" name="id" value="@supporter.Id" />
                                            <button type="submit" class="admin-btn admin-btn-danger admin-btn-sm">
                                                <i class="fas fa-trash-alt"></i> Xóa
                                            </button>
                                        </form>
                                    </div>
                                </td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="8" class="admin-empty-state">
                                <i class="fas fa-inbox"></i>
                                <h4>Không có dữ liệu</h4>
                                <p>Không tìm thấy người hỗ trợ nào phù hợp với tiêu chí tìm kiếm.</p>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

        <!-- Pagination -->
        @if (Model.TotalPages > 1)
        {
            <nav aria-label="Page navigation" class="admin-pagination">
                <ul class="pagination mb-0">
                    <li class="page-item @(!Model.HasPreviousPage ? "disabled" : "")">
                        <a class="page-link admin-page-link" href="@Url.Action("ManageSupporters", new { 
                            page = Model.PageNumber - 1, 
                            pageSize = Model.PageSize,
                            searchString = ViewBag.SearchString,
                            filterApproved = ViewBag.FilterApproved
                        })">
                            <i class="fas fa-chevron-left"></i> Trước
                        </a>
                    </li>
                    
                    @for (int i = Math.Max(1, Model.PageNumber - 2); i <= Math.Min(Model.TotalPages, Model.PageNumber + 2); i++)
                    {
                        <li class="page-item @(i == Model.PageNumber ? "active" : "")">
                            <a class="page-link admin-page-link" href="@Url.Action("ManageSupporters", new { 
                                page = i, 
                                pageSize = Model.PageSize,
                                searchString = ViewBag.SearchString,
                                filterApproved = ViewBag.FilterApproved
                            })">@i</a>
                        </li>
                    }
                    
                    <li class="page-item @(!Model.HasNextPage ? "disabled" : "")">
                        <a class="page-link admin-page-link" href="@Url.Action("ManageSupporters", new { 
                            page = Model.PageNumber + 1, 
                            pageSize = Model.PageSize,
                            searchString = ViewBag.SearchString,
                            filterApproved = ViewBag.FilterApproved
                        })">
                            Sau <i class="fas fa-chevron-right"></i>
                        </a>
                    </li>
                </ul>
            </nav>
            
            <div class="text-center text-muted mt-3">
                <i class="fas fa-info-circle me-2"></i>
                Hiển thị <strong>@((Model.PageNumber - 1) * Model.PageSize + 1)</strong> - 
                <strong>@(Math.Min(Model.PageNumber * Model.PageSize, Model.TotalItems))</strong> 
                trong tổng số <strong>@Model.TotalItems</strong> kết quả
            </div>
        }
    </div>
</div>
