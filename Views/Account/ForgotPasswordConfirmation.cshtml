@{
    ViewData["Title"] = "Đã gửi email";
}

<div class="container mt-5 mb-5">
    <div class="row justify-content-center">
        <div class="col-md-6 col-lg-5">
            <div class="auth-card">
                <div class="auth-header text-center">
                    <div style="font-size: 4rem; margin-bottom: 1rem;">📧</div>
                    <h2 class="mb-0">Đã gửi email</h2>
                    <p class="text-muted mt-2">Vui lòng kiểm tra hộp thư của bạn</p>
                </div>

                <div class="auth-content">
                    <div class="alert alert-info" role="alert">
                        <i class="fas fa-info-circle me-2"></i>
                        <strong>Hướng dẫn:</strong>
                        <ul class="mb-0 mt-2">
                            <li>Kiểm tra hộp thư đến của email bạn đã nhập</li>
                            <li>Nếu không thấy email, vui lòng kiểm tra thư mục <strong>Spam</strong> hoặc <strong>Thư rác</strong></li>
                            <li>Click vào link trong email để đặt lại mật khẩu</li>
                            <li>Link sẽ hết hạn sau <strong>1 giờ</strong></li>
                        </ul>
                    </div>

                    <div class="text-center mt-4">
                        <a asp-action="Login" class="btn btn-outline-primary">
                            <i class="fas fa-arrow-left me-2"></i>Quay lại đăng nhập
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .auth-card {
        background: white;
        border-radius: 20px;
        box-shadow: 0 10px 40px rgba(0, 0, 0, 0.1);
        padding: 2.5rem;
        margin-top: 2rem;
    }

    .auth-header h2 {
        font-size: 2rem;
        font-weight: 700;
        color: #2c3e50;
    }

    .auth-content {
        margin-top: 2rem;
    }

    .alert {
        border-radius: 12px;
        border: none;
    }

    .btn-outline-primary {
        border: 2px solid #FF6B9D;
        color: #FF6B9D;
        border-radius: 12px;
        padding: 0.875rem 1.5rem;
        font-weight: 600;
        transition: all 0.3s ease;
    }

    .btn-outline-primary:hover {
        background: #FF6B9D;
        color: white;
        transform: translateY(-2px);
        box-shadow: 0 8px 20px rgba(255, 107, 157, 0.3);
    }
</style>

